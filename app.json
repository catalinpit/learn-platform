{
  "scripts": {
    "dokku": {
      "predeploy": "echo 'Running prisma generate...' && bun run prisma generate && echo 'Running prisma migrate deploy...' && bun run prisma migrate deploy"
    }
  }
}
