{
  "scripts": {
    "dokku": {
      "predeploy": "cd /app && echo 'Running prisma generate...' && bun run prisma generate || exit 1 && echo 'Running prisma migrate deploy...' && bun run prisma migrate deploy || exit 1"
    }
  }
}
